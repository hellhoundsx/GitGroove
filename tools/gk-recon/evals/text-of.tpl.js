(() => { const el = document.querySelector('__SEL__'); if (!el) return 'missing __SEL__'; const b = el.getBoundingClientRect(); return `<${el.tagName.toLowerCase()}.${(el.className||'').toString().split(/\s+/).slice(0,5).join('.')}> ${Math.round(b.width)}x${Math.round(b.height)} @${Math.round(b.x)},${Math.round(b.y)}\n` + el.innerText.replace(/\n{2,}/g, '\n').slice(0, 1800) + '\n\nbuttons: ' + [...el.querySelectorAll('button')].map(b => b.innerText.replace(/\s+/g, ' ').trim() || '(' + (b.getAttribute('title') || b.getAttribute('aria-label') || b.getAttribute('data-testid') || 'icon') + ')').slice(0, 40).join(' | ') + '\ninputs: ' + [...el.querySelectorAll('input,textarea,select')].map(i => i.type + ':' + (i.placeholder || i.name || i.id || '')).slice(0, 20).join(' | '); })()
